server {
    listen <%= ENV["PORT"] %>;

    server_name <%= ENV['HEROKU_APP_NAME'] %>.herokuapp.com;

    location / {
        root /app;
        index index.html;
        try_files $uri $uri/ =404;
    }
}
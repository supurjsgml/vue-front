# worker_processes 1;

# events { worker_connections 1024; }

# http {
#   include       mime.types;
#   default_type  application/octet-stream;

#   sendfile        on;
#   keepalive_timeout  65;

#   server {
#     listen 80;

#     server_name vue-front-807ffc6b572e.herokuapp.com;

#     location / {
#       # 만약 특정 경로로의 리다이렉트가 필요하면 아래의 리다이렉트 라인을 유지하세요.
#       return 301 https://guney.kro.kr$request_uri;
      
#       # 또는 원래의 프록시 설정을 사용하려면, return 301 라인을 주석 처리하고 아래의 프록시 설정을 사용하세요.
#       # proxy_pass http://127.0.0.1:<%= ENV['PORT'] %>;
#       # proxy_set_header Host $host;
#       # proxy_set_header X-Real-IP $remote_addr;
#       # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#       # proxy_set_header X-Forwarded-Proto $scheme;
#     }
#   }
# }
apiVersion: v1
kind: Service
metadata:
  name: vue-front-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: vue-front
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000

